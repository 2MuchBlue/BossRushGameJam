using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


public class BetterButtonNavigator : MonoBehaviour
{
    [ExecuteAlways]
    [SerializeField]
    Button upwardTargetButton;
    [SerializeField]
    Button downwardTargetButton;
    [SerializeField]
    Button leftTargetButton;
    [SerializeField]
    Button rightTargetButton;

    public Navigation nav;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
